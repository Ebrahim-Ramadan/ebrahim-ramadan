/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = {
  env: {
    GITHUB_BAT_ACCESS_TOKEN : "github_pat_11ALUE2DEFYmHN7Tdc",
  },

    images: {
      domains: ['http.cat'], 
    },
  };
