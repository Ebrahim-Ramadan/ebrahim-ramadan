/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
  publicRuntimeConfig: {
    GH_API_KEY: 'github_pat_/....',
  },
    images: {
      domains: ['http.cat'], 
    },
  };